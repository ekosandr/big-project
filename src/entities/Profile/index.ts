import { profileActions, profileReducer } from './modal/slice/profileSlice';
import { Profile, ProfileSchema } from './modal/types/profile';

export { Profile, ProfileSchema, profileActions, profileReducer };
